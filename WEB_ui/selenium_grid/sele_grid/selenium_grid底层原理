selenium通过发送一个命令到CPU，然后会下达一个指令到浏览器。

那么如何发送指令到其他电脑中呢？

selenium有一个总线机制，及设置一个电脑位主节点。其他的电脑位设为node节点
让主节点与分支节点的通信即可。

先启动一个主节点（理解成一个程序即可），开放自己端口与ip，然后以字典的形式保存核心的数据
然后其他的节点则是通过网络的传输，传输到hub

java -jar selenium-server-4.23.1.jar hub 主节点 port=4444